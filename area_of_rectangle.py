def area(length, breadth):
    # Calculate the area
    area_ = length * breadth
    return area_

# Example usage:
length = int(input("Enter the length: "))
breadth = int(input("Enter the breadth: "))
print("Area:", area(length, breadth))  # This will print the calculated area
